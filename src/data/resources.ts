import { Resource } from '@/types/resource';

// Resources are now fetched from the database
// Providers can upload custom resources via the Provider Dashboard
export const resources: Resource[] = [];
